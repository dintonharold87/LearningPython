mytrial.py
year=int(year)
    if (year>=1) or (year<=9999):
        if(year%4)==0:
            if(year%100)==0:
                if(year%400):
                    b="The year is a leap year (it has 366 days)"
                    return b
                else:
                     a="The year is not a leap year(it has 365 days)"
                     return a
                    
                     
            else:
                b="The year is a leap year (it has 366 days)"
                return b
        else:
            a="The year is not a leap year(it has 365 days)"
            return a
    else:
        return False
    